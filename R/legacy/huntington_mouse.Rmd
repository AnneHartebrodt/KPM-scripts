---
title: "Chorea huntington"
output: html_document
---

Test whether the approach is likely to work by using a pathway which is
known in order to calculate the test statistics. 

```{r import}
require(data.table)
require(ggplot2)

```

# relevant genes
```{r}
chorea_genes<-fread("/home/anne/Documents/Master/MA/data/Kegg_Huntington_EntrezID_Mouse.csv")
chorea_genes<-as.data.table(unique(unlist(chorea_genes)))
write.table(chorea_genes, file = "/home/anne/Documents/Master/MA/data/Kegg_Huntington_EntrezID_Mouse_unique.csv", sep="\t", col.names = F, row.names = F)

# Map genes

mapping<-fread("/home/anne/Documents/Master/MA/data/mouseEntrez_ENS.txt")
mapping<-mapping[!is.na(mapping$`NCBI gene ID`)]
m<-which(mapping$`NCBI gene ID` %in% mapping$`NCBI gene ID`)
mapping[m]
```